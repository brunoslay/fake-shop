name: CI-CD
on:
  push:
    branches: ["main"]
  workflow_dispatch:
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obter código"
      - run: echo "Executar o docker build"
      - run: echo "Enviar a image a imagem docker para o docker hub"
  CD:
    needs: [CI]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obter código"
      - run: echo "Configurar o kube config"
      - run: echo "Executar o apply"
